pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/SreekardesikJetti/hellojenkins'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "No Maven project — skipping mvn build"'
                // Replace with your actual build script, like:
                // sh 'npm install' for Node.js
                // or sh './build.sh' if you have a script
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Run your tests here"'
                // For example: sh 'npm test' or ./run-tests.sh
            }
        }
    }
}

